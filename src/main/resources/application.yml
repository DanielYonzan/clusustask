spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fx-deals
    username: test
    password: Test@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: info
    com.example.fxdeals: debug
    org.hibernate.sql: debug
    org.hibernate.type.descriptor.sql.BasicBinder: Trace
